"""Built-in agents shipped with the platform.

Each agent module exposes a ``register_*`` function that registers both
the agent metadata and the tools it relies on with the module-level
registries in :mod:`app.modules.ai_agents.base`.
"""
